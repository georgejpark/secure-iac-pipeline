# Copy to .env and fill in. .env is gitignored and must never be committed.
DATABASE_URL=postgresql://user:CHANGEME@localhost:5432/claims
AWS_REGION=us-east-2
# Optional. Without it, AI triage falls back to deterministic rules.
ANTHROPIC_API_KEY=
